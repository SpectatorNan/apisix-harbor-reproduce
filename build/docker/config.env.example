# Harbor Docker Registry 配置
# 复制此文件为 config.env 并填写实际值
# 注意: config.env 已在 .gitignore 中，不会被提交到 Git

# Harbor 服务器地址（不包含 https://）
HARBOR_URL="harbor.example.com"

# Harbor 项目名称
HARBOR_PROJECT="library"

# Harbor 认证信息
# 推荐使用 Robot Account 而不是普通用户账号
# 格式: robot$<account-name>
HARBOR_USERNAME="admin"
HARBOR_PASSWORD="Harbor12345"

# 或使用 Robot Account (推荐用于 CI/CD)
# HARBOR_USERNAME="robot\$github-actions-bot"
# HARBOR_PASSWORD="eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCJ9..."

# Docker 构建选项
# 目标平台（多平台构建）
PLATFORMS="linux/amd64,linux/arm64"

# 使用 Docker Buildx 进行多平台构建
USE_BUILDX=true

# 是否同时推送 latest 标签
PUSH_LATEST=false

# 额外的构建参数（可选）
# BUILD_ARGS="--build-arg BASE_IMAGE=debian:12 --build-arg VERSION=1.0.0"

# 是否禁用缓存
NO_CACHE=false
