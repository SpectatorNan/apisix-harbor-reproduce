# Helm Chart 仓库配置
# 复制此文件为 config.env 并填写实际值
# 注意: config.env 已在 .gitignore 中，不会被提交到 Git

# Chart 仓库类型（仅支持 OCI）
CHART_REPO_TYPE="oci"

# Chart 仓库 URL (OCI 格式)
# 例如: oci://harbor.example.com/library
CHART_REPO_URL="oci://harbor.example.com/library"

# 仓库认证信息
# 推荐使用 Robot Account 而不是普通用户账号
CHART_REPO_USERNAME="admin"
CHART_REPO_PASSWORD="Harbor12345"

# 或使用 Robot Account (推荐用于 CI/CD)
# CHART_REPO_USERNAME="robot\$github-actions-bot"
# CHART_REPO_PASSWORD="eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCJ9..."

# Chart 打包选项
# 是否更新依赖
UPDATE_DEPENDENCIES=true

# 是否自动递增版本号（patch 版本）
AUTO_INCREMENT_VERSION=false

# 输出目录（默认为仓库根目录）
OUTPUT_DIR="../.."

# Chart 签名（可选）
SIGN_CHART=false

# GPG 签名密钥（如果 SIGN_CHART=true）
# SIGN_KEY="your-gpg-key-id"
# SIGN_KEYRING="~/.gnupg/secring.gpg"

# 是否在打包前 lint Chart
LINT_CHART=true
